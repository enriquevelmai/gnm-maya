"""VIEW + CONTROLLER layer: the Qt panel, dialogs, and Maya UI helpers.

``widgets`` are the reusable slider widgets, ``panel`` is the main tabbed
window (its handlers are the controllers translating user input into
``core.head`` calls), ``tools`` holds the menu-launched dialogs (presets,
crowd, export, landmarks, gallery), and ``progress``/``shelf``/``licenses``
are small Maya UI utilities.
"""
